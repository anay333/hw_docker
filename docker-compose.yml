version: '3.1'
services:
  postgresql:
    image: postgres
    ports:
      - '9999:5432'
    volumes:
      - ./data://var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=13032022
      - POSTGRES_DB=my_super_db
